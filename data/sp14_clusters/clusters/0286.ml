AppG [VarG,AppG [EmptyG]]
bigAdd value (snd a)
bigAdd value (snd a)
clonehelper x (abs n)
clonehelper x (abs n)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
pi ** eval (a , x , y)
mulByDigit i (List.rev l)
mulByDigit i (List.rev t)
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
mulByDigit i
           (List.rev (List.map (fun x ->
                                  x * 10) t))
