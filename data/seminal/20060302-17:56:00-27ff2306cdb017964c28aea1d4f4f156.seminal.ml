let rec reverseHelp len place str =
  let firstChar = str.[place] in
  str

let rec reverseHelper str = 
  let temp = str in
  temp.[0] = 'z'

let reverse str =
  let currChar = str.[0] in 
  str

let _ = print_string(reverseHelper "fast")