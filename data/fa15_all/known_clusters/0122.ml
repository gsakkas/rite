CaseG (AppG (fromList [EmptyG])) (fromList [(Nothing,LitG)])
match explode x with
| h :: t -> false
match explode x with
| h :: t -> false
match explode x with
| h :: t -> false
match explode x with
| h :: t -> false
match explode x with
| h :: t -> false
match explode x with
| h :: t -> false
match explode x with
| h :: t -> false
