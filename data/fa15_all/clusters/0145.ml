IteG (BopG EmptyG EmptyG) (ListG EmptyG Nothing) (LetG NonRec (fromList [EmptyG]) EmptyG)
if l = []
then []
else (let h :: t = l in
      match h with
      | 0 -> removeZero t
      | _ -> l)
if l = []
then []
else (let h :: t = l in
      match h with
      | 0 -> removeZero t
      | _ -> l)
if l = []
then []
else (let h :: t = l in
      match h with
      | 0 -> removeZero t
      | _ -> l)
if l = []
then []
else (let h :: t = l in
      match h with
      | 0 -> removeZero t
      | _ -> l)
