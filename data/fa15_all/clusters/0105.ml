TupleG (fromList [LitG,LetG NonRec (fromList [EmptyG]) EmptyG])
(1 , let (y , z) = x in
     [((y + z) + carry) mod 10] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(1 , let (y , z) = x in
     [((y + z) + carry) mod 10] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(1 , let (y , z) = x in
     [((y + z) + carry) mod 10] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(1 , let (y , z) = x in
     [((y + z) + carry) mod 10] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
(1 , let (y , z) = x in
     [((y + z) + carry) mod 10] @ ans)
(0 , let (y , z) = x in
     [(y + z) + carry] @ ans)
