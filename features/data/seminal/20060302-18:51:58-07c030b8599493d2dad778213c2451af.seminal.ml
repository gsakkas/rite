let rec reverseHelp len place str =
  String.make(1, 't')

let rec reverseHelper str = 
  "" ^ "r"

let reverse str =
  let currChar = str.[0] in 
  str

let test  = "test"
let _ = print_string(test.[0].string)

let _ = print_string(reverseHelp 4 0 "fast")
